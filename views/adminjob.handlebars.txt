<div class="main-body">
    <div class="update-form-section">
        <form id="update-job-form">
            <div class="form-group">
                <input type="text" name="title" id="title" class="form-control">
            </div>
            <div class="form-group">
                <select name="type" id="type" class="form-control">
                    <option value="0">--Select--</option>
                    {{#each types}}
                        <option value="{{this.id}}">{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="form-group">
                <select name="company" id="company" class="form-control">
                    <option value="0">--Select--</option>
                    {{#each companies}}
                    <option value="{{this.id}}">{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="form-group">
                <textarea name="description" placeholder="description" id="description" cols="30" rows="10" class="formcontrol"></textarea>
            </div>
            <div class="form-group">
                <button class="btn-primary">Udate</button>
            </div>
        </form>
    </div>
    <div class="create-form-section">
         <form action="/job/add" method="post">
            <div class="form-group">
                <input type="text" name="title" id="" class="form-control">
            </div>
            <div class="form-group">
                <select name="type" id="" class="form-control">
                    <option value="0">--Select--</option>
                    {{#each types}}
                        <option value="{{this.id}}">{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="form-group">
                <select name="company" id="" class="form-control">
                    <option value="0">--Select--</option>
                    {{#each companies}}
                    <option value="{{this.id}}">{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="form-group">
                <textarea name="description" placeholder="description" id="" cols="30" rows="10" class="formcontrol"></textarea>
            </div>
            <div class="form-group">
                <button class="btn-primary">Create</button>
            </div>
        </form>
    </div>
</div>
<div class="table-section">
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Type</th>
                <th>Company</th>
                <th>Description</th>
                <th colspan="2">Action</th>
            </tr>
        </thead>
        <tbody>
            {{#each jobs}}
                <tr>
                    <td>{{this.type}}</td>
                    <td>{{this.title}}</td>
                    <td>{{this.type}}</td>
                    <td>{{this.company}}</td>
                    <td>{{this.description}}</td>
                    <td><button class="btn-alert">Edit</button></td>
                    <td><button class="btn-danger">Delete</button></td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>